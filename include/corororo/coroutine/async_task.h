#pragma once

#include <corororo/coroutine/promise.h>
// Forward declare AsyncTask to avoid circular dependency
#include <corororo/coroutine/types.h>

#include <coroutine>
#include <exception>
#include <utility>

namespace CoroRoro
{

// AsyncTaskPromise definitions moved to task.h to avoid circular dependencies

} // namespace CoroRoro